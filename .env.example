# 复制成 .env（/lab-start 会自动填好 LLM 部分）。.env 已被 .gitignore 挡掉，绝不提交。

# ── LLM（/lab-start 自动注入，你不需要自己的 AWS/Anthropic 密钥）──
PARALLIGHT_API_KEY=
PARALLIGHT_BASE_URL=https://token.parallight.ai/api/llm
PARALLIGHT_MODEL=claude-sonnet-4-6

# ── GitHub MCP 的 token（给最小权限：public_repo 只读就够这节课）──
# 在 https://github.com/settings/tokens 生成 fine-grained token，只勾只读权限。
GITHUB_PERSONAL_ACCESS_TOKEN=

# ── 投递渠道（在 config.yaml 选 channel；只需配你选的那个）──

# 渠道 A：Gmail（课堂演示默认 / 在国外推荐）
# ⚠️ 不是登录密码！先开两步验证，再到 https://myaccount.google.com/apppasswords 生成「应用专用密码」。
GMAIL_ADDRESS=
GMAIL_APP_PASSWORD=
MAIL_TO=                      # 收件人；留空就发给自己(GMAIL_ADDRESS)
# 换别的邮箱服务商就改这几个（默认是 Gmail）：
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# 渠道 B：飞书自定义机器人（在大陆推荐）。这个 URL 本身就是凭据！绝不进 git/截图。
# 飞书群 → 设置 → 群机器人 → 添加「自定义机器人」生成。
FEISHU_WEBHOOK_URL=
FEISHU_WEBHOOK_SECRET=        # 只在机器人开了「签名校验」时填；开「关键词」就留空
